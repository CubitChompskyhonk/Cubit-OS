# Deprecated stub — use android-apk.yml (Build CubitOS free APK).
# Kept as a no-op so old links do not fail the repo status.
name: Android CI

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Redirect
        run: |
          echo "Use workflow 'Build CubitOS free APK' (.github/workflows/android-apk.yml)."
          echo "This stub does not build."
