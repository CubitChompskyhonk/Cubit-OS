# Core: Python 3.10+ stdlib-first
# Optional / web
openai>=1.0.0
pydantic>=2.0.0
fastapi>=0.110.0
uvicorn>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.9
# Optional commerce (desktop/server only — not used by free Android APK)
stripe>=8.0.0
